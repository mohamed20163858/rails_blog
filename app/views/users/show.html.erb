<div id="<%= @user.id %>" class="card">
  <div class="img"><%= image_tag "https://i.ibb.co/CP4m1b4/img.jpg", alt: "user-photo", height:150 %></div>
  <div class="info">
    <h2>User name: <%= @user.name %></h2>
    <p>Number of posts: <%= @user.posts_counter %></p>
  </div>
</div>
<div class="posts">
  <div class="bio">
    <h2>Bio</h2>
    <p><%= @user.bio %></p>
  </div>
  <% @user.posts.each_with_index do |post, i| %>
    <div class="post" id="<%= post.id %>" onclick="redirectFunction(<%= post.id %>)">
    <h2><%= post.title %></h2>
    <p><%= post.text %></p>
    <p class="reactions">Comments: <%= post.comments_counter %>, Likes: <%= post.likes_counter %> </p>
    </div>
  <% end %>
  <button onclick="seePostsFunction(<%= @user.id %>)">See all posts</button> 
  <%if @user.id == @current_user.id %>
    <button onclick="createPostFunction(<%=@user.id%>)">Create post</button>
  <%end%>
</div>
<script>
  function seePostsFunction(id) {
     window.location.href = id+"/posts";
  }
  function createPostFunction(id) {
    window.location.href = "/posts/new"
  }
  function redirectFunction (id) {
  window.location.href += "/posts/" + id;
  }
</script>